To run the program, type make or make all within the directory the makefile is into compile the executables. 
As for what is needed, you must have the openssl libraries downloaded onto your machine via 
"sudo apt-get install -y libssl-doc libssl-dev" and you can choose to run either executable for weak/strong resistance testing.
